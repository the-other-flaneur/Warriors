{
  "name": "gamedev",
  "version": "1.0.0",
  "main": "app/dist/app.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server/dist/app.js",
    "build:frontend": "tsc -p client/tsconfig.json",
    "build:backend": "tsc -p server/tsconfig.json",
    "build": "npm run build:frontend && npm run build:backend",
    "dev:frontend": "tsc -p client/tsconfig.json -w",
    "dev:backend": "nodemon --watch 'server/**/*.ts' --exec 'ts-node' server/app.ts",
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "watch": "concurrently \"tsc -p client/tsconfig.json -w\" \"tsc -p server/tsconfig.json -w\""
  },
  "dependencies": {
    "express": "^4.21.2",
    "socket.io": "^4.8.1",
    "socket.io-client": "^4.8.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.10.2",
    "@types/socket.io": "^3.0.1",
    "concurrently": "^9.1.0",
    "nodemon": "^3.1.9",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.2"
  }
}
